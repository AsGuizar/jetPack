<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetPack"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/jetPack"
            android:theme="@style/Theme.JetPack">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CalculatorActivity"
            android:label="@string/calculator_activity_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AddToListActivity"
            android:label="@string/AddToListActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".CheckboxCalculatorActivity"
            android:label="@string/CheckboxCalculatorActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".NumSumActivity"
            android:label="@string/NumSumActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".FruitCollectionActivity"
            android:label="@string/FruitCollectionActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>
